# Vulnerable Dependencies

Supply-chain exercises should distinguish presence from reachability and
production impact. Future labs can pin intentionally vulnerable crates here
without making them dependencies of the DVRA application itself.
