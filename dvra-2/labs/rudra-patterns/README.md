# Rudra Patterns

This directory is reserved for small, original exercises inspired by Rudra
pattern categories such as `SendSyncVariance`, `UninitExposure`,
`HigherOrderInvariant`, and `PanicSafety`.

The MVP implements the first two pattern-style exercises in
`crates/unsafe-cache` instead of copying old third-party proof-of-concept code.
